export const notificationPropNames = ['title', 'message', 'duration']

export const transitionTime = 475

export const dummyKeyframes = [{ opacity: 1 }, { opacity: 1 }]

export const messages = {
  timeout: 'The notification closed automatically because no action was taken in time.'
}
