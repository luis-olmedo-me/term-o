import { configIds, configInputIds, configSections } from './useConfig.constants'
import { useConfig } from './useConfig.hook'

export default useConfig

export { configIds, configInputIds, configSections, useConfig }
