import styled from 'styled-components'

export const Input = styled.input`
  padding: 6px 10px;
  width: -webkit-fill-available;
  border: none;
  border-radius: 3px;
  font-family: Coda;
  font-size: 1em;
  background: #00000050;

  &:active,
  &:focus,
  &:focus-visible {
    outline: none;
  }
`
